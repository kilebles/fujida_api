services:
  fujida_api:
    build: .
    container_name: fujida_api
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: >
      uvicorn app.fujida_api.main:app
      --host 0.0.0.0
      --port 8000
    volumes:
      - .:/app